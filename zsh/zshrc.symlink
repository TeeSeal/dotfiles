export DOTFILES="$HOME/.dotfiles"

files=($DOTFILES/*/*.zsh)
for file in $files; do
  source "$file"
done

# Load and configure plugins
[ -f "$DOTFILES/antibody/load" ] && source  "$DOTFILES/antibody/load"

# Source localrc
[ -f "$HOME/.localrc" ] && source "$HOME/.localrc"
